class AppConstants {
  // ✅ For Flutter Web (Chrome), use localhost:3000
  // If running on another device, replace with your machine IP: e.g., http://192.168.1.100:3000
  static const String baseUrl = "http://localhost:3000";

  static const roles = ["end_user", "receiver"];
  static const statuses = ["Pending", "Confirmed", "Partially Fulfilled"];
}
